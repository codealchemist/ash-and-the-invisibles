import styled from 'styled-components'

const Spacer = styled.div`
  border-bottom: 1px dashed var(--color-1);
  width: 370px;
  height: 1px;
  margin: 50px 0 10px 0;
  filter: contrast(50) brightness(0.3);
`

export default Spacer
